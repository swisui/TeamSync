import { FormatRelativePipe } from './format-relative.pipe';

describe('FormatReleativePipe', () => {
  it('create an instance', () => {
    const pipe = new FormatRelativePipe();
    expect(pipe).toBeTruthy();
  });
});
