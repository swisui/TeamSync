import { IconFilterPipe } from './icon-filter.pipe';

describe('IconFilterPipe', () => {
    it('create an instance', () => {
        const pipe = new IconFilterPipe();
        expect(pipe).toBeTruthy();
    });
});
