import { GetColorPipe } from './get-color.pipe';

describe('GetColorPipe', () => {
    it('create an instance', () => {
        const pipe = new GetColorPipe();
        expect(pipe).toBeTruthy();
    });
});
